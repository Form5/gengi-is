ul.currency-list.js-currency-list(ng-repeat='item in items.results')
  li
    a(ng-href='/#/{{item.shortName}}')
      span.list-left(class='{{item.shortName}}')
      hgroup
        h2 {{item.shortName}}
        h3 {{item.longName}}