.row
  .col.large-6.center
    ul.currency-list(class='{{listView}}')
      li(ng-repeat='item in items')
        a(ng-href='/#/{{item.shortName}}')
          span.list-left
          span.symbol(class='{{item.shortName}}')
          h2 {{item.shortName}}
          h3 {{item.longName}}
